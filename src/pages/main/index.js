import { memo } from 'react';

import './style.css';


const Main = (props) => {

    return (
        <main className='main-container'>
            Main
        </main>
    )
}

export default memo(Main);
